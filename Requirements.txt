# Meditation Proctor System - Production Requirements
# Python 3.8+ required

ultralytics==8.3.236
torch==2.0.0
torchvision==0.15.0
opencv-python==4.11.0.86
mediapipe==0.10.21
numpy==1.26.4
scipy==1.16.3
reportlab==4.4.7
yt-dlp==2025.12.08
python-dotenv==1.0.0



# ============================================
# NOTES FOR DEPLOYMENT
# ============================================
# GPU: Requires NVIDIA GPU with CUDA 12.1
# If deploying on CPU-only machine, replace torch lines with:
#   torch==2.5.1
#   torchvision==0.20.1
# 
# System Dependencies (install separately):
#   - FFmpeg (for video processing)
#   - Visual C++ Redistributable (Windows)

# # Step 1: Install FFmpeg (ONE TIME)
# choco install ffmpeg              # Windows
# # OR
# sudo apt-get install ffmpeg       # Linux

# # Step 2: Setup Python Environment
# python -m venv venv
# venv\Scripts\activate             # Windows
# source venv/bin/activate          # Linux

# # Step 3: Install Python Packages
# pip install -r requirements.txt

# # Step 4: Configure
# # Create .env file with correct paths

# # Step 5: Run
# python main.py
# ```

# ---

# ## Your Complete Deployment Package:

# 📦 **Files to Upload to Server:**
# ```
# ├── requirements.txt              # Python packages
# ├── system-dependencies.txt       # FFmpeg install commands
# ├── setup.ps1 or setup.sh        # Automated setup (optional)
# ├── test_ffmpeg.py               # Verify FFmpeg works
# ├── check_versions.py            # Verify all packages
# ├── .env.example                 # Template for configuration
# ├── Dockerfile                   # If using Docker (optional)
# └── [all your project files]



# how to download ffmpeg and set up 

# https://www.gyan.dev/ffmpeg/builds/

# Above is tthe link to copy command to install ffmpeg


# while installing ffmpeg as we are working on 64bit ARM we cannot install it using "winget" so we are going to install it using chocolatey.

# here are the steps to follow while installing it

# first to install chocolatey:
# You need to Run powershell as administrator, next you'll be pasting below command:
# "Set-ExecutionPolicy Bypass -Scope Process -Force; `                                             >> [System.Net.ServicePointManager]::SecurityProtocol = `                                                               >> [System.Net.ServicePointManager]::SecurityProtocol -bor 3072; `                                                      >> iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))"


# ########################################################################################################
# in powershell
# ########################################################################################################
# Set-ExecutionPolicy Bypass -Scope Process -Force
# [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
# iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
# #########################################################################################################
# choco install ffmpeg
# #########################################################################################################
# #########################################################################################################

# make sure to remove string quotes.

# then you will visit the gyan.dev link provided above and from there you'll copy the command

# "choco install ffmpeg"    ## avoid quotes



# After installing ffmpeg make sure you give proper path to your ffmpeg package to backend code.

# @IN CMD
# You can use this command "where ffmpeg" to know the path it will give path example below:
# "C:\ProgramData\chocolatey\bin\ffmpeg.exe"

# AFter this add this path to ffmpeg path in code.